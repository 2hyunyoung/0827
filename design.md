# 월간 발주계획 시스템 디자인 가이드

> 이 문서는 한국후지필름BI 월간 발주계획 시스템의 화면 디자인 기준입니다. 새 화면을 만들거나 기존 화면을 개편할 때 먼저 읽고, 기존 `app/globals.css`의 토큰과 클래스를 우선 사용합니다.

## 1. 디자인 방향

### 한 문장 정의

밝고 정돈된 운영 대시보드 안에서 공급·재고·발주 위험을 빠르게 발견하고 다음 행동으로 이어지게 하는 업무용 시스템입니다.

### 참고 화면에서 가져오는 핵심 요소

- 좌측 고정 내비게이션과 상단 유틸리티 바
- 밝은 캔버스 위에 흰색 카드가 놓이는 표면 계층
- 파란색을 주행동과 선택 상태에 사용
- 초록·주황·빨강으로 운영 상태를 즉시 구분
- 요약 지표 → 상세 목록 → 개별 조치로 이어지는 정보 구조
- 숫자와 상태를 넉넉한 여백, 짧은 라벨, 진행률로 빠르게 읽는 방식

### 제품에 맞춘 원칙

1. **업무 우선** — 장식보다 이번 달 발주 판단에 필요한 정보가 먼저 보입니다.
2. **근거가 보이는 숫자** — 지표에는 기간, 표본 수, 계산 기준 또는 데이터 출처를 함께 표시합니다.
3. **상태는 색과 문구로 함께 표현** — 색만으로 안전·주의·위험을 구분하지 않습니다.
4. **한국어 우선** — 화면 문구, 도움말, 오류 메시지, 주석은 한국어로 작성합니다. 코드 식별자와 DB 컬럼명만 영어를 사용합니다.
5. **일관성 우선** — 화면마다 새로운 카드, 버튼, 색을 만들지 않고 공통 클래스를 재사용합니다.

## 2. 브랜드와 시각 언어

### 제품 표기

- 제품명: `한국후지필름BI 월간 발주계획`
- 화면 제목은 사용자가 판단할 업무를 명확히 말합니다. 예: `재고 소진 위험`, `리드타임 격차`, `월간 발주량`
- 참고 HTML의 `LogiCore`, `Warehouse Management`, `Add Facility` 같은 임시 영문 표현은 그대로 사용하지 않습니다.

### 분위기

신뢰감 있는 B2B 운영 도구의 인상을 유지합니다. 차갑고 과도하게 장식적인 SaaS 화면보다 차분한 네이비 사이드바, 흰색 작업 영역, 선명한 파란색 액션을 기본으로 합니다.

### 아이콘

- 아이콘은 의미를 보조할 때만 사용합니다.
- 아이콘만 단독으로 두지 말고 버튼의 accessible name 또는 인접 텍스트를 제공합니다.
- 새 아이콘 라이브러리를 추가하지 않습니다. 현재 프로젝트에서 사용하는 아이콘 방식이 있다면 그것을 따릅니다.

## 3. 색상 토큰

기준값은 `app/globals.css`의 `:root` 변수입니다. 컴포넌트 안에 임의의 색상값을 직접 넣지 않습니다.

| 역할 | 토큰 | 사용처 |
|---|---|---|
| 기본 글자 | `--ink` | 제목, 본문, 주요 숫자 |
| 보조 글자 | `--muted` | 설명, 단위, 메타 정보 |
| 화면 배경 | `--canvas` | 전체 페이지 바탕 |
| 작업 표면 | `--surface` | 카드, 상단 바, 표 영역 |
| 브랜드 주색 | `--navy` | 사이드바, 강조 제목 |
| 주요 액션 | `--blue` | 선택 상태, 주요 버튼, 링크 |
| 긍정/정상 | `--green` | 완료, 안전, 개선 |
| 주의 | `--amber` | 표본 부족, 확인 필요, 경고 |
| 위험 | `--red` | 소진 임박, 조회 실패, 위험 |
| 구분선 | `--line` | 카드와 표의 테두리 |

상태 표현은 다음 규칙을 따릅니다.

- `SAFE` / 정상: `tag green`, `text-good`
- `CRITICAL` / 위험: `tag red`, `text-danger`
- 확인 필요 또는 표본 부족: `tag amber`, `metric-foot warn`
- `UNKNOWN`, 계산 불가, 미확정: `tag gray`, `muted`
- 위험 여부가 문맥상 중요한 숫자는 색상뿐 아니라 `위험`, `안전`, `계산 불가` 같은 텍스트를 함께 표시합니다.

## 4. 글꼴과 숫자

- 기본 글꼴은 `Noto Sans KR`입니다. 현재 `app/globals.css`의 선언을 유지합니다.
- 제목은 굵고 짧게, 설명은 한 단계 작고 muted 색으로 둡니다.
- 지표 숫자는 `metric-value`를 사용합니다.
- 수량, 일수, 퍼센트, 금액 등 비교가 중요한 값은 오른쪽 정렬합니다.
- 숫자에는 단위를 붙이되, 단위가 반복되는 표에서는 헤더에 단위를 적고 셀에서는 생략할 수 있습니다.
- 계산 불가능한 값은 `0`, `999`, 임의의 대체 숫자로 채우지 않고 `—`와 사유 문구를 사용합니다.

## 5. 화면 구조

### 데스크톱 기본 구조

```text
app-shell
├── sidebar       브랜드와 단계/분석 메뉴
└── main
    ├── topbar    현재 화면, 기간, 로컬 상태 등
    └── content   progress-wrap 또는 analysis-page + 본문
```

- 사이드바는 현재 구조를 유지하고 폭을 화면마다 바꾸지 않습니다.
- 상단 바는 현재 위치와 데이터 기준일을 보여줍니다.
- 본문은 `content` 또는 분석 화면의 `analysis-page`를 사용합니다.
- 한 화면의 최상단에는 `eyebrow` → 제목 → 설명 순서를 권장합니다.

### 분석 화면 구조

```text
analysis-shell
├── analysis-topbar
│   ├── analysis-home
│   └── analysis-tabs
└── analysis-page
    ├── analysis-heading
    ├── 핵심 KPI: grid grid-3 또는 grid grid-4
    ├── 판단/설명 카드: card, callout
    └── 상세 결과: section card + analysis-table-wrap
```

분석 화면은 `app/analysis/leadtime/page.tsx`의 구조를 본보기로 삼습니다. 조회 함수는 화면에 넣지 않고 `lib/scm.ts`에 둡니다.

## 6. 컴포넌트 사용 규칙

### 카드와 KPI

- 기본 카드: `card`
- 카드 제목 영역: `card-title`
- KPI 카드: `card metric`
- KPI 라벨: `metric-label`
- KPI 값: `metric-value`
- 보조 설명: `metric-foot`
- KPI는 한 화면에서 3~4개를 우선 사용합니다. 숫자만 나열하지 말고 무엇을 의미하는지 짧은 설명을 붙입니다.
- 참고 HTML의 카드 hover 상승 효과는 상호작용 가능한 카드에만 적용합니다. 단순 정보 카드는 움직이지 않습니다.

### 버튼

- 기본 버튼: `button`
- 가장 중요한 한 가지 행동: `button primary`
- 보조 링크형 행동: `button ghost`
- 여러 버튼은 `button-row`로 묶고, 한 화면의 primary 버튼은 가능한 한 하나로 제한합니다.
- 버튼 문구는 `저장`, `확정`, `다음 단계`, `상세 보기`처럼 동작을 명확히 씁니다.
- 파괴적 행동은 빨간 primary 버튼을 남용하지 말고 확인 절차와 경고 문구를 함께 제공합니다.

### 배지와 상태

상태 배지는 `tag`에 상태 클래스를 조합합니다. 예: `tag green`, `tag amber`, `tag red`, `tag gray`.

- 배지는 짧은 명사형 상태를 표시합니다.
- 상태를 설명하는 긴 문장은 배지 옆 또는 카드 아래에 둡니다.
- 상태 정의가 화면마다 달라지지 않도록 `SCHEMA.md`의 값(`SAFE`, `CRITICAL`, `UNKNOWN`)을 그대로 사용합니다.

### 표

- 일반 결과 표는 `table-wrap`과 `table`을 사용합니다.
- 분석 결과 표는 `analysis-table-wrap`과 `analysis-table`을 사용합니다.
- 표 헤더는 연한 배경, 행은 얇은 구분선으로 구분합니다.
- 숫자는 `num` 클래스로 오른쪽 정렬합니다.
- 긴 표는 페이지 전체를 넓히지 말고 표 영역 안에서 가로 스크롤합니다.
- 행 hover는 선택 가능한 행의 위치를 알려주는 용도로만 사용합니다.
- 빈 결과와 조회 오류는 반드시 다르게 표현합니다.
  - 오류: `조회에 실패했습니다.` + 원인 또는 점검 안내
  - 빈 결과: `표시할 데이터가 없습니다.`

### 설명과 경고

- 일반 안내: `callout blue`
- 확인이 필요한 안내: `callout`
- 데이터 기준, 표본 수, 계산 불가 사유는 표 위 또는 해당 지표 근처에 둡니다.

## 7. 상태·상호작용·접근성

- hover만으로 핵심 정보를 전달하지 않습니다. 키보드 focus에서도 동일한 상태를 확인할 수 있어야 합니다.
- 모든 버튼과 링크는 키보드로 접근 가능해야 하며, focus outline을 제거하지 않습니다.
- 입력 요소에는 label을 연결합니다. placeholder를 label 대신 사용하지 않습니다.
- 표의 헤더와 셀 관계가 명확해야 합니다. 숫자 정렬만으로 의미를 전달하지 않습니다.
- 로딩, 오류, 빈 결과, 계산 불가를 각각 구분합니다.
- 애니메이션은 짧고 보조적이어야 하며, 값의 의미를 가리거나 반복 주의를 끌지 않습니다.
- 색 대비가 낮은 연한 글자를 본문에 사용하지 않습니다.
- 모바일에서 표는 가로 스크롤을 허용하되, 핵심 판단 지표는 카드나 요약 문장으로 먼저 제공합니다.

## 8. 반응형 기준

- 데스크톱: 사이드바 + 본문 2열/3열 그리드
- 태블릿: `grid-4`를 2열로 줄이고, 툴바는 줄바꿈 허용
- 모바일: 사이드바를 상단 영역으로 전환하고, `grid-4`, `grid-3`, `grid-2`는 1열로 쌓습니다.
- 고정 폭, 고정 높이의 지도·패널을 새로 만들 때는 작은 화면에서 콘텐츠가 잘리지 않는지 확인합니다.
- `app/globals.css`의 기존 media query를 우선 활용하고, 꼭 필요한 경우에만 파일 끝에 최소한의 규칙을 추가합니다.

## 9. 구현 규칙

- Tailwind, styled-components, CSS Modules, 새 UI 프레임워크를 추가하지 않습니다.
- 스타일은 `app/globals.css`의 공통 클래스와 변수로 관리합니다.
- 새 화면은 다음 순서로 만듭니다.
  1. `lib/scm-model.ts`에 타입과 정규화
  2. `lib/scm.ts`에 조회 함수
  3. `app/analysis/<기능이름>/page.tsx`에 화면
  4. 필요할 때만 공통 컴포넌트 또는 `app/globals.css` 확장
- 화면 컴포넌트에서 평균, 분위수 등 업무 계산을 하지 않습니다. 계산은 SQL 뷰 또는 모델 함수에서 처리합니다.
- 화면 문구는 한국어로 쓰고, DB 원본 `raw`를 직접 조회하지 않습니다. 화면은 원칙적으로 `analytics`를 조회합니다.
- 새 화면은 `export const dynamic = 'force-dynamic'`을 검토해 데이터 캐시로 최신 상태가 가려지지 않게 합니다.
- 변경 후에는 `npm run build`를 실행합니다.

## 10. 화면 작성 체크리스트

### 구조

- [ ] 기존 `app-shell`, `sidebar`, `topbar`, `content` 또는 `analysis-*` 구조를 사용했는가?
- [ ] 제목만 읽어도 화면의 업무 목적을 알 수 있는가?
- [ ] KPI → 상세 표 → 다음 행동 순서가 자연스러운가?

### 시각

- [ ] 색상은 `app/globals.css` 토큰과 기존 클래스로 표현했는가?
- [ ] primary 버튼이 너무 많지 않은가?
- [ ] 상태가 색상과 텍스트로 함께 표현되는가?
- [ ] 숫자와 단위, 기준일 또는 표본 수가 명확한가?

### 데이터와 오류

- [ ] `raw`가 아닌 `analytics` 또는 허용된 `core` 조회를 사용했는가?
- [ ] 조회 오류와 빈 결과를 구분했는가?
- [ ] 계산 불가를 임의의 숫자로 대체하지 않았는가?
- [ ] 한 건의 결과와 극단값을 사람이 검증할 수 있는가?

### 검증

- [ ] 모바일과 데스크톱에서 레이아웃이 깨지지 않는가?
- [ ] 키보드 focus와 입력 label을 확인했는가?
- [ ] `npm run build`가 통과하는가?

