import AnalysisFrame from '@/components/analysis/analysis-frame';
import DemandProfileTable from '@/components/analysis/demand-profile-table';
import KpiCard from '@/components/ui/kpi-card';
import Panel from '@/components/ui/panel';
import { getDemandProfile, getDemandProfileKpi } from '@/lib/scm';
export const dynamic = 'force-dynamic';
export default async function DemandProfilePage() { const [{ rows, error: rowsError }, { data: kpi, error: kpiError }] = await Promise.all([getDemandProfile(), getDemandProfileKpi()]); return <AnalysisFrame title="SKU 수요 패턴" description="학습 구간의 전체 월 Grid로 SKU별 수요 특성을 분류합니다. 검증 구간은 계산에 사용하지 않습니다.">{rowsError || kpiError ? <div className="card"><p className="text-danger">조회에 실패했습니다.</p><p className="muted">{rowsError ?? kpiError}</p></div> : !kpi ? <div className="card"><p className="muted">KPI 결과가 없습니다. analytics.v_demand_profile_kpi와 Exposed schemas를 확인하세요.</p></div> : <><div className="grid grid-4"><KpiCard label="전체 SKU" value={kpi.totalItems} foot="학습 Grid 기준" /><KpiCard label="Croston 후보" value={kpi.nCrostonNeeded} foot="INTERMITTENT + LUMPY" tone="warning" /><KpiCard label="계산 불가" value={kpi.nCalculationUnavailable} foot="reason code 확인" tone="critical" /><KpiCard label="SMOOTH" value={kpi.nSmooth} foot={`INTERMITTENT ${kpi.nIntermittent} · ERRATIC ${kpi.nErratic} · LUMPY ${kpi.nLumpy}`} tone="safe" /></div><div className="section card"><div className="card-title"><h3>SKU Demand Profile</h3><span>학습 기간 데이터만 사용</span></div>{rows.length === 0 ? <p className="muted">표시할 데이터가 없습니다. analytics.v_sku_demand_profile과 Exposed schemas를 확인하세요.</p> : <DemandProfileTable rows={rows} />}</div></>}</AnalysisFrame>; }
